const readlineSync = require("readline-sync");
 const number = Number(readlineSync.question("Give me number:"))
let sum = 0;
for (i = 0; i < 99; i++) {
   sum = sum + (i + 1);
   NumberSum = sum + number;
}
const avg = NumberSum / number;
console.log(`+ Extra number.The average is ${avg} `);
